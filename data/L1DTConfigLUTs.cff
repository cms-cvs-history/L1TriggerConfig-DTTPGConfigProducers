block LUTParametersBlock = {
  PSet LUTParameters = {

    untracked int32 Debug = 1	# Debug flag 
    int32 BTIC = 31   # BTIC parameter
    double D = 431.	# distance vertex to normal, unit cm. only for test: CHAMBER DEPENDENT ! 
    double XCN = -80.  # distance vertex to normal, unit cm. only for test CHAMBER DEPENDENT ! 
    int32 WHEEL = -1 # wheel sign (-1 or +1)
  }

}
